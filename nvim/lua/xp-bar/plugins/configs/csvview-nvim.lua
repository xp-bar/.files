require('csvview').setup({
    view = {
        display_mode = "border",
        min_column_width = 6,  -- Minimum width for each column
        spacing = 2,           -- Space between columns
    },
})
